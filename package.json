{
  "name": "hellodev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HelloDev - Developer dashboard Chrome extension",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "happy-dom": "^17.0.0",
    "@playwright/test": "^1.50.0"
  }
}
